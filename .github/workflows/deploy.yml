name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          VITE_FIREBASE_API_KEY: "AIzaSyAp9UJYdU7-Dxo-4_emNIMiIaiz42XF3EM"
          VITE_FIREBASE_AUTH_DOMAIN: "oroluxstor-b2b15.firebaseapp.com"
          VITE_FIREBASE_PROJECT_ID: "oroluxstor-b2b15"
          VITE_FIREBASE_STORAGE_BUCKET: "oroluxstor-b2b15.firebasestorage.app"
          VITE_FIREBASE_MESSAGING_SENDER_ID: "978253542057"
          VITE_FIREBASE_APP_ID: "1:978253542057:web:9546e01981f07c8c73afb8"
          VITE_FIREBASE_MEASUREMENT_ID: "G-5FQT34W88Y"

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
          clean: true
